import { FilterJsonPipe } from './filter-json.pipe';

describe('FilterJsonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterJsonPipe();
    expect(pipe).toBeTruthy();
  });
});
