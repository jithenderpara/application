import { FilterdescPipe } from './filterdesc.pipe';

describe('FilterdescPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterdescPipe();
    expect(pipe).toBeTruthy();
  });
});
